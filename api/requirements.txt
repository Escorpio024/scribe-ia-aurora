# --- Core API ---
fastapi==0.112.1
uvicorn[standard]==0.30.5
httpx==0.27.0
pydantic==2.8.2
python-multipart==0.0.9

# --- Audio / ASR ---
faster-whisper==1.0.1
ctranslate2==4.4.0
numpy==1.26.4

# --- Mejora de audio (alineación y robustez) ---
# whisperx = extiende Whisper con alineación fonética, multi-speaker diarization
whisperx==3.1.3
# torchaudio = para procesamiento de audio con PyTorch (resample, denoise, augmentations)
torchaudio==2.3.0
# pytorch (necesario para whisperx y torchaudio; ajusta la versión a tu GPU/CPU)
torch==2.3.0